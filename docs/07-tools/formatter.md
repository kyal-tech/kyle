# Formatter

**Status:** Available

## Usage

```bash
ky fmt <file.ky> # Format a single file
ky fmt <directory/> # Format all .ky filis in a directory
ky fmt --check # Check formatting without modifying (CI mode)
ky fmt src/ tests/ # Format project sources
```
