# LSP — Language Server Protocol

**Status:** Available

## Usage

```bash
ky lsp
```

The LSP server communicatis over stdin/stdout. It is used by the VS Code extension and any editor supporting LSP.

## Features

- Diagnostics (type errors, undefined symbols)
- Autocomplete
- Go-to-definition
- Hover type information
- Find references
- Rename symbol
- Inlay hints (inferred types)
- Code lens (run test)
